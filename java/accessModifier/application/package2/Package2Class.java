/*
package accessModifier.application.package2;

import application.package1.PublicClass;

public class Package2Class extends PublicClass {

    void testAccess() {
        System.out.println(publicVar);  // ✅ Allowed (public)
        System.out.println(protectedVar);  // ✅ Allowed (protected via inheritance)
        //System.out.println(defaultVar);  // ❌ Not Allowed (default is package-private)
        //System.out.println(privateVar);  // ❌ Not Allowed (private is class-private)
    }

}
*/
